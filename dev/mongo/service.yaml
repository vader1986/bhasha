apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
spec:
  type: NodePort
  selector:
    app: db
  ports:
    - port: 27017 # container port of MongoDB pod
      nodePort: 30101 # exposed port (used for connection string)