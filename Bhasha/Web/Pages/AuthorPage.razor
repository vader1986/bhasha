@page "/author"
@inherits Bhasha.Web.Pages.UserPage

<AuthorizeView Roles="Admin, Author">
    <PageTitle>Select or create chapter</PageTitle>

    @if (_error != null)
    {
        <MudText>@_error</MudText>
    }
    
    @if (DisplayOverview)
    {
        <MudButton OnClick="@OnEditChapterClicked">Edit Chapter</MudButton>
        <MudButton OnClick="@OnCreateChapterClicked">Create Chapter</MudButton>
    }

    @if (DisplayChapterList)
    {
        <ChapterSelection ChapterSelected="@OnChapterSelected" />
    }
    
</AuthorizeView>