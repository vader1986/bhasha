@if (_audioFileName is not null)
{
    <audio id="@_id" src="@_audioFileName" autoplay>
        Your browser does not support the audio element.
    </audio>
}

@if (_debug is not null)
{
    <MudText>@_debug</MudText>
}