@typeparam T

<MudSelect T="string" Label="@Label" @bind-Value="_selectedValue" @bind-Value:after="@OnValueChanged">
    
    @foreach (var value in Values)
    {
        <MudSelectItem Value="@value">@value</MudSelectItem>
    }

</MudSelect>